<resources>
    <!-- AppCompat parent is required for the per-app locales backport (MainActivity : AppCompatActivity) -->
    <style name="Theme.Orderak" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
