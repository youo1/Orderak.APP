# Gradle files
.gradle/
build/
build-errors.txt

# Dependency and generated workspace state
node_modules/
.scratch/
.scratch-*/
outputs/
_temp_*
*.artifact.md

# Wrangler runs from services/backend/ — ignore any stray root-level Wrangler artifacts
.wrangler/
worker-configuration.d.ts
!/apps/admin-web/src/edge/worker-configuration.d.ts

# Local configuration file (sdk path, etc)
local.properties

# Log/OS Files
*.log
~$*.docx

# Android Studio generated files and folders
captures/
.externalNativeBuild/
.cxx/
*.apk
output.json

# IntelliJ
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Keystore files
*.jks
*.keystore

# SSH keys
.ssh/

# Google Services (e.g. APIs or Firebase)
google-services.json

# Android Profiling
*.hprof

.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Generated documentation site
/site/

# Generated city-catalogue imports (large, reproducible from the pinned source)
/services/backend/generated/

# VS Code MCP configuration
.vscode/mcp.json

# Docker volumes
docker-compose.override.yml


node_modules

# Python virtual environments, caches, and build metadata
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/

# Turborepo local cache
.turbo/

# Schemathesis writes its example database next to wherever it is invoked,
# which for the contract jobs is the repository root.
.schemathesis/

# Build and vendor drops. Several tools in this repository emit into a dist/
# directory — the OpenAPI bundler, and the design-system export the admin-web
# tokens are copied from. None of it is source: it is either regenerated by a
# build or duplicated into the app that consumes it, so a committed copy can
# only drift from the thing it was generated out of.
dist/
